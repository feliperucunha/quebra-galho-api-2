{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "knex --knexfile knexfile.ts migrate:latest && node dist/src/server.js",
    "knex:migrate": "knex --knexfile knexfile.ts migrate:latest",
    "knex:migrate:rollback": "knex --knexfile knexfile.ts migrate:rollback",
    "build": "tsc",
    "dev": "tsnd --transpile-only --ignore-watch node_moludes --respawn src/server.ts"
  },
  
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@types/cors": "^2.8.7",
    "@types/express": "^4.17.7",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "knex": "^0.21.5",
    "sqlite3": "^5.0.0",
    "ts-node-dev": "^1.0.0-pre.59"
  }
}
